<UserControl x:Class="BayWyn.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reports" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Today's Courier Report" Width="170" Height="35" Margin="0,0,10,0"
                    Command="{Binding TodayCourierReportCommand}"/>
            <Button Content="Current Month Jobs" Width="170" Height="35" Margin="0,0,10,0"
                    Command="{Binding CurrentMonthJobsCommand}"/>
            <Button Content="Monthly Value Report" Width="170" Height="35"
                    Command="{Binding MonthlyValueReportCommand}"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{Binding Summary}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding ReportJobs}" Height="300"/>
        </StackPanel>
    </Grid>
</UserControl>