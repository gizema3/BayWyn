<UserControl x:Class="BayWyn.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,20,0,16">
            <WrapPanel> <!--Generate report-->
                <Button Content="Today's Courier Report"
                        Width="190"
                        Margin="0,0,10,10"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding TodayCourierReportCommand}"/>
                <Button Content="Current Month Jobs"
                        Width="170"
                        Margin="0,0,10,10"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding CurrentMonthJobsCommand}"/>
                <Button Content="Monthly Value Report"
                        Width="170"
                        Margin="0,0,10,10"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding MonthlyValueReportCommand}"/>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Summary" Style="{StaticResource SectionTitleText}"/>
                    <TextBlock Text="{Binding Summary}"
                               Margin="0,10,0,0"
                               FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="Report Data" Style="{StaticResource SectionTitleText}"/>
                        
                    </StackPanel>

                    <DataGrid Grid.Row="1" AutoGenerateColumns="True" ItemsSource="{Binding ReportJobs}"/> <!-- Display reportjobs and create columns automatically based on object properties-->
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>