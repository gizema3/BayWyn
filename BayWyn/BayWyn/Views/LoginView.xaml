<Window x:Class="BayWyn.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="320" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BayWyn Couriers Login" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Username"/>
        <TextBox Grid.Row="1" Margin="0,25,0,10" Height="30"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Text="Password"/>
        <PasswordBox Grid.Row="2" Margin="0,25,0,10" Height="30" Name="PasswordBox"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10"/>

        <Button Grid.Row="4" Content="Login" Width="100" Height="35"
                HorizontalAlignment="Left" Command="{Binding LoginCommand}"/>
    </Grid>
</Window>